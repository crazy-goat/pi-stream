version: "2"

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true

output:
  formats:
    colored-line-number:
      print-issued-lines: true
      print-linter-name: true

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - errcheck
    - dupl
  settings:
    staticcheck:
      checks:
        - "all"
        - "-QF*"
        - "-ST*"

formatters:
  enable:
    - gofmt

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
